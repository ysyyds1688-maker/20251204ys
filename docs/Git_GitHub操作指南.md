# Git/GitHub 操作完整指南

## 📋 概述

本指南整合了所有 Git 和 GitHub 相關的操作說明，包括上傳指南、檢查報告、常見問題等。

---

## 🚀 上傳步驟

### 1. 初始化 Git（如果還沒初始化）

```bash
git init
```

### 2. 設定 Git 用戶資訊（首次使用）

```bash
git config --global user.email "your.email@example.com"
git config --global user.name "Your Name"
```

### 3. 加入所有檔案

```bash
git add .
```

### 4. 檢查會上傳的檔案（確認沒有敏感資訊）

```bash
git status
```

### 5. 提交

```bash
git commit -m "Initial commit: YS 娛樂論壇前端原型"
```

### 6. 連接到 GitHub 並推送

```bash
git remote add origin <你的 GitHub repo URL>
git branch -M main
git push -u origin main
```

---

## ✅ 應該上傳的檔案和資料夾

### 📁 核心檔案

- `package.json` - 依賴管理
- `package-lock.json` - 鎖定依賴版本（**必須上傳**）
- `tsconfig.json` - TypeScript 配置
- `vite.config.ts` - Vite 配置
- `index.html` - HTML 入口
- `index.tsx` - React 入口
- `App.tsx` - 主應用程式
- `README.md` - 專案說明

### 📁 程式碼

- `components/` - 所有共用組件
- `pages/` - 所有頁面組件
- `src/` - 源碼（services, data）
- `public/` - 靜態資源
  - `public/images/` - **所有壓縮後的圖片**（24MB）
  - `public/videos/` - 影片檔案
  - `public/robots.txt`
  - `public/sitemap.xml`

### 📁 文件

- `docs/` - **所有技術文件**

### 📁 配置檔案

- `.gitignore` - Git 忽略規則
- `zeabur.json` - Zeabur 部署配置

---

## ❌ 不需要上傳（會被 .gitignore 忽略）

- `node_modules/` - 依賴套件（執行 `npm install` 會自動安裝）
- `dist/` - 建置產物（部署時會重新 build）
- `.env` / `.env.local` - 環境變數（包含敏感資訊）
- `.DS_Store` - macOS 系統檔案
- `*.log` - 日誌檔案
- 編輯器設定檔（`.vscode/`, `.idea/`）

---

## ⚠️ 注意事項

### 1. 不要上傳敏感資訊

- `.env` 檔案（如果有的話）
- API Keys
- 個人資訊

### 2. 確認圖片已壓縮

- 目前 `public/images/` 約 24MB（已壓縮）
- 如果太大，GitHub 可能會警告
- 單個檔案超過 100MB 會被拒絕

### 3. package-lock.json 必須上傳

- 確保團隊成員安裝相同版本的依賴
- 確保部署環境使用正確的依賴版本

### 4. 不要上傳 dist/

- 這是 build 產物，部署時會重新生成
- 避免版本衝突

---

## 📊 預估上傳大小

- 程式碼：~2-3 MB
- 圖片：~24 MB（已壓縮）
- 影片：取決於 `YS89.com.mp4` 大小
- 文件：~50 KB
- **總計：約 30-50 MB**（不含 node_modules）

---

## ✅ GitHub 專案檢查清單

### 核心檔案 ✓

- [ ] `.gitignore` - 已上傳
- [ ] `package.json` + `package-lock.json` - 已上傳
- [ ] `tsconfig.json` + `vite.config.ts` - 已上傳
- [ ] `index.html` + `index.tsx` + `App.tsx` - 已上傳
- [ ] `README.md` - 已上傳

### 程式碼結構 ✓

- [ ] `components/` - 已上傳
- [ ] `pages/` - 已上傳
- [ ] `src/` - 已上傳
- [ ] `docs/` - 已上傳

### 靜態資源 ✓

- [ ] `public/robots.txt` + `public/sitemap.xml` - 已上傳
- [ ] `public/videos/` - 已上傳
- [ ] `public/images/` - 已上傳（所有圖片）

### 安全性 ✓

- [ ] 沒有上傳 `node_modules/`
- [ ] 沒有上傳 `dist/`
- [ ] 沒有上傳 `.env.local`
- [ ] 沒有上傳 `.DS_Store`

---

## 🔧 常見問題

### Q1: Git 認證失敗

**錯誤**：
```
fatal: could not read Username for 'https://github.com': Device not configured
```

**解決方案**：
1. 使用 Personal Access Token（推薦）
2. 或使用 SSH 認證
3. 或在 GitHub Desktop 中操作

### Q2: 檔案太大無法上傳

**錯誤**：
```
remote: error: File is too large
```

**解決方案**：
1. 壓縮圖片（已壓縮到 24MB）
2. 使用 Git LFS（Large File Storage）
3. 或將大檔案放在 CDN

### Q3: 中文檔名問題

**解決方案**：
- Git 支援中文檔名
- 如果遇到問題，檢查 Git 配置：
  ```bash
  git config --global core.quotepath false
  ```

### Q4: 提交後發現漏了檔案

**解決方案**：
```bash
# 加入漏掉的檔案
git add <檔案路徑>

# 修改最後一次提交
git commit --amend --no-edit

# 強制推送（如果已經推送過）
git push -f origin main
```

---

## 📝 常用 Git 指令

### 基本操作

```bash
# 查看狀態
git status

# 查看變更
git diff

# 查看提交歷史
git log

# 查看遠端倉庫
git remote -v
```

### 提交和推送

```bash
# 加入所有變更
git add .

# 提交
git commit -m "提交訊息"

# 推送到遠端
git push origin main

# 拉取遠端變更
git pull origin main
```

### 分支操作

```bash
# 建立新分支
git branch <分支名稱>

# 切換分支
git checkout <分支名稱>

# 合併分支
git merge <分支名稱>
```

---

## 🔗 相關文件

- `部署完整指南.md` - 部署說明
- `README.md` - 專案說明

---

## 💡 最佳實踐

1. **定期提交**：不要累積太多變更才提交
2. **清晰的提交訊息**：說明這次提交做了什麼
3. **不要提交敏感資訊**：使用環境變數
4. **保持 .gitignore 更新**：確保不會意外提交不需要的檔案
5. **使用分支**：重要功能使用分支開發

